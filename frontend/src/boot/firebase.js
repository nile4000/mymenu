import { boot } from "quasar/wrappers";

// Firebase initialisation is currently disabled but the boot file is kept
// so that it can be easily enabled later on.
export default boot(() => {
  // Intentionally left blank
});
